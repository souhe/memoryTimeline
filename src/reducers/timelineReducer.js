

const initialState = {
    events: []
};

export default function timelineReducer(state = initialState, action = {}){
    switch(action.type){
        case '': 
            return state;
        default:
            return state;
    }
}